[package]
name = "waproto"
version = "0.1.0"
edition = "2024"
authors = ["João Lucas <jlucaso@hotmail.com>"]
license = "MIT"
repository = "https://github.com/jlucaso1/whatsapp-rust"
description = "Protocol Buffers definitions for WhatsApp"

[dependencies]
prost = { version = "0.14.1", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
reflect-macros = { path = "../reflect-macros", version = "0.1.0" }
reflect-metadata = { path = "../reflect-metadata", version = "0.1.0" }

[build-dependencies]
prost-build = { version = "0.14.1", default-features = false }
